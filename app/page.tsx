import Gallery from "@/components/Gallery";

const Home = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between px-20 py-12 pt-40 bg-black font-primary">
      <Gallery />
    </main>
  );
};

export default Home;
