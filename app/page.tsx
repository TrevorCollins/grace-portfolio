import Gallery from "@/components/Gallery";

const Home = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between px-10 md:px-20 py-12 pt-40 bg-black">
      <Gallery />
    </main>
  );
};

export default Home;
